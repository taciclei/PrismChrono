```mermaid
classDiagram
    class PreloadPipeline {
        - loaded: Vec<usize>
        + new() -> PreloadPipeline
        + preload(keys: Vec<usize>) -> Vec<usize>
    }
    PreloadPipeline : stub de pipeline de préchargement IA
```
